import { PeoplefilterPipe } from './peoplefilter.pipe';

describe('PeoplefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PeoplefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
